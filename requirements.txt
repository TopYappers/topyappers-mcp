# Local development dependencies (not used by Cloudflare Workers)
# The Worker runs on Pyodide and uses only Python standard library + JS interop.
#
# These are useful if you want to test the API client logic locally:
httpx>=0.27.0
